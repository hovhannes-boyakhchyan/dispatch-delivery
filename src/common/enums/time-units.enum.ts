export enum TimeUnitsEnum {
  years = 'years',
  quarters = 'quarters',
  month = 'month',
  months = 'months',
  weeks = 'weeks',
  days = 'days',
  hours = 'hours',
  minutes = 'minutes',
  seconds = 'seconds',
  milliseconds = 'milliseconds',
}
