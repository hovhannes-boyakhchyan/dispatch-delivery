export enum DeliveryDetailsStatusEnum {
  PENDING = 'pending',
  PICKUP = 'pickup',
  PICKUP_COMPLETE = 'pickup_complete',
  DROPOFF = 'dropoff',
  DELIVERED = 'delivered',
  CANCELED = 'canceled',
  RETURNED = 'returned',
}
